'use strict'

angular.module('dappstackApp.common.admin')

    .controller('AdminController', ['ngDialog', 'authService', '$localStorage', function (ngDialog, authService, $localStorage) {
        
        var vm = this;

        
    }]);
